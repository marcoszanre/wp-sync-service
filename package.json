{
  "name": "wp-sync-service",
  "version": "1.0.0",
  "description": "Service to sync WP users from CSV",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "csv-parser": "^3.0.0",
    "dotenv": "^10.0.0"
  }
}
